{
  "name": "countdown-timer",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && vite preview"
  },
  "devDependencies": {
    "vite": "^2.7.2"
  }
}
